:to="
            databaseName === 'daily'
              ? {
                  name: 'daily-detail',
                  params: {
                    locale: Trans.currentLocale,
                    slug: tour.tourSlug[Trans.currentLocale].current,
                  },
                }
              : {
                  name: 'central-detail',
                  params: {
                    locale: Trans.currentLocale,
                    slug: tour.tourSlug[Trans.currentLocale].current,
                  },
                }
          "